#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
    /* using puts to print */
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
